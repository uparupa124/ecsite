<h1>ショッピングカート</h1>
 <%= form_with model: @cart_items, url: cart_item_path(@cart_item.id), local: true do |f| %>
　<table>
　  <thead>
　    <tr>
　      <th>商品名</th>
　      <th>単価(税込)</th>
　      <th>数量</th>
　      <th>小計</th>
　      <th></th>
　    </tr>
　  </thead>
　    <tbody>
　     <% @cart_item.each do |cart_item| %>
　      <tr>
　        <td><%= cart_item.item.name %></td>
　        <td><%= (cart_item.item.price * 1.1).round %></td>
　        <td><%= f.select :amount, options_for_select(@numbers, {selected: cart_item.amount} ) %><%= f.submit "変更" %></td>
　        <td><%= (cart_item.item.price * 1.1 * cart_item.amount).round %></td>
　        <!--<td>しますか?" %></td>-->
　      </tr>
　     <% end %>
　    </tbody>
　</table>
 <% end %>